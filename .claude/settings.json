{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath' | { read -r f; case \"$f\" in *.py) cd /home/ntnguyen/workspace/github.com/sirtaylor88/asteroids-game-using-pygame && uv run ruff check --select I --fix \"$f\";; esac; } 2>/dev/null || true",
            "statusMessage": "Sorting imports..."
          }
        ]
      }
    ]
  }
}
